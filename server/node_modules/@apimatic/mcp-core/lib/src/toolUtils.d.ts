import { type CallToolResult, type Tool } from '@modelcontextprotocol/sdk/types.js';
import type { CoreClient, EndpointMetadataInterface } from '@apimatic/metadata-interfaces';
export type ToolDefinition = {
    tool: Tool;
    handler: (arg: unknown) => Promise<CallToolResult>;
};
/**
 * Creates a tool definition from a given endpoint.
 */
export declare function createToolFromEndpoint(endpointId: string, endpoint: EndpointMetadataInterface<any, any>, sdkClient: CoreClient): ToolDefinition;
//# sourceMappingURL=toolUtils.d.ts.map