import type { CoreClient, EndpointsObject } from '@apimatic/metadata-interfaces';
import { Server } from '@modelcontextprotocol/sdk/server/index.js';
/**
 * Creates and configures a new `Server` instance with tool support capabilities,
 * prepares all tools from the provided endpoints, and sets up request handlers for
 * listing tools and calling tools.
 */
export declare function getServer(serverName: string, endpoints: EndpointsObject, sdkClient: CoreClient, toolsets: string[]): Server;
//# sourceMappingURL=mcpServer.d.ts.map